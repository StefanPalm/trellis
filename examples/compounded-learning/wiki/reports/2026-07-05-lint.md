---
type: report
title: Lint report — 2026-07-05
description: First lint pass — conformance and integrity clean; one contested page with a proposed experiment; four open questions.
created: 2026-07-05
updated: 2026-07-05
---

# Lint report — 2026-07-05

First lint pass over the bundle (7 concept pages, 4 topics, 2 archived
sources). Findings by pass, severity-ordered within each.

## 1. Conformance

Clean. All non-reserved files carry valid frontmatter with `type`; tags
reference charter topics; reserved files follow OKF (root index carries
`okf_version` only; log is newest-first); all in-body links are absolute and
resolve.

## 2. Integrity

Clean. No orphans — every concept page is indexed and has at least one
inbound link. Both intake records match the pages they name.

## 3. Taxonomy drift

No action needed. `open-questions` has held zero pages since creation
(2026-07-01); this is expected for a four-day-old wiki, but if it is still
empty at the next lint, demote it or start feeding it (see pass 6).

## 4. Epistemic health

- [/practices/ingest-batch-size.md](/practices/ingest-batch-size.md) is
  `contested` with a resolution path already on the page: one week of
  immediate ingestion vs one week of batching, judged by the next lint's
  duplicate findings. **Recommend running the experiment** — a retro note per
  week is enough source material.
- [/foundations/llm-wiki-pattern.md](/foundations/llm-wiki-pattern.md) and
  [/foundations/open-knowledge-format.md](/foundations/open-knowledge-format.md)
  are `provisional` on a single reading-notes source. One primary source each
  (Karpathy's original post; the OKF spec itself) would promote them.

## 5. Contradiction sweep

None found beyond the already-marked contested page.

## 6. Coverage and growth

**Gaps:** "the lint report as a reading list" is asserted on two pages but
has no page of its own; "gap notes" (the wiki-ask loop) are undocumented
entirely.

**Questions the wiki cannot yet answer, but its purpose says it should:**

1. Does batched ingestion measurably beat immediate ingestion (fewer
   duplicates at next lint), and where is the freshness line? — resolvable by
   the pass-4 experiment.
2. What is the minimum lint cadence for a high-volume week (10+ inbox items)?
3. What makes a gap note good enough that the next ingest can act on it
   without the original question's context?
4. When, if ever, should the archive be pruned — and what does provenance
   cost when it is?

**Leads:** a second practitioner's first-month retro (independent evidence
for the `provisional` practice pages); the OKF spec itself as a primary
source; any published llm-wiki write-up with more than a week of use behind
it.

## Recommendations

1. Run the ingest-batch-size experiment (pass 4) — it resolves the wiki's
   only contested page and question 1.
2. Feed `open-questions`: questions 2–4 above are each one ingest batch.
3. Add primary sources for the two provisional foundations pages.
